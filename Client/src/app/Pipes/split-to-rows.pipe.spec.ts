import { SplitToRowsPipe } from './split-to-rows.pipe';

describe('SplitToRowsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitToRowsPipe();
    expect(pipe).toBeTruthy();
  });
});
